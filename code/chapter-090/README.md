# Chapter 090 — Multi-Agent Platform

Production-style project package `multiagent` (offline mocks for CI).

```bash
cd code/chapter-090 && pytest -q && python3 main.py
docker build -t ch090-multiagent .
```
