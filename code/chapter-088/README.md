# Chapter 088 — Customer Support Agent

Production-style project package `support` (offline mocks for CI).

```bash
cd code/chapter-088 && pytest -q && python3 main.py
docker build -t ch088-support .
```
