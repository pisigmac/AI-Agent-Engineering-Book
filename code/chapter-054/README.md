# Chapter 054 — Semantic Kernel
Offline teaching adapter for **Semantic Kernel** concepts (no vendor lock-in required for tests).
```bash
cd code/chapter-054 && pytest -q && python3 main.py
```
Maps framework ideas onto the Part IV/V platform ports: tools, graphs, crews, typed results, plugins, runners.
