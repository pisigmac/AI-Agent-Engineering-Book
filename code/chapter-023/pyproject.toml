[project]
name = "ai-agent-platform-chapter-023"
version = "1.0.0"
requires-python = ">=3.10"
dependencies = [
  "chromadb>=0.5.0",
]

[project.optional-dependencies]
dev = ["pytest>=8.0"]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
