[project]
name = "ai-agent-platform-chapter-010"
version = "1.0.0"
description = "Chapter 10 token calculator and context packer"
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=8.0"]
tiktoken = ["tiktoken>=0.7"]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
