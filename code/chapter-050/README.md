# Chapter 050 — LangGraph
Offline teaching adapter for **LangGraph** concepts (no vendor lock-in required for tests).
```bash
cd code/chapter-050 && pytest -q && python3 main.py
```
Maps framework ideas onto the Part IV/V platform ports: tools, graphs, crews, typed results, plugins, runners.
